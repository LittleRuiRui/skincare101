import React, { useEffect, useMemo, useState } from "react";
import { AlertTriangle, CheckCircle2, FileText, History, RefreshCw, ShieldAlert } from "lucide-react";
import { supabase } from "../lib/supabase";
import { loadMyRoutines, type RoutineListItem } from "../lib/routines";
import { useLanguage } from "../lib/i18n";

const INK = "#211F1B";
const LINE = "#DDD6CA";
const SAGE = "#718276";
const MUTE = "#777065";
const RUST = "#A8503A";

type Section = "feedback" | "posts";
type HistoryFilter = "all" | "avoid" | "better" | "neutral";
type ExperienceRow = { id:string; product_key:string; brand:string; product_name:string; skin_type:string; sensitivity:string; concerns:string[]; reaction:"better"|"neutral"|"irritated"; texture:"love"|"okay"|"dislike"; repurchase:"yes"|"maybe"|"no"; note:string; updated_at:string; };
function isAvoid(row:ExperienceRow){return row.reaction==="irritated"||row.texture==="dislike"||row.repurchase==="no";}

export default function MyProductHistory(){
 const{t}=useLanguage();
 const[section,setSection]=useState<Section>("feedback"),[rows,setRows]=useState<ExperienceRow[]>([]),[posts,setPosts]=useState<RoutineListItem[]>([]),[filter,setFilter]=useState<HistoryFilter>("all"),[loading,setLoading]=useState(true),[error,setError]=useState("");
 async function load(){setLoading(true);setError("");try{const[{data,error:rpcError},routineRows]=await Promise.all([supabase.rpc("load_my_product_experiences"),loadMyRoutines()]);if(rpcError)throw rpcError;setRows((data||[]) as ExperienceRow[]);setPosts(routineRows);}catch(err:any){setError(err?.message||t("暂时无法读取账号记录。","Could not load your account history."));}finally{setLoading(false)}}
 useEffect(()=>{void load()},[]);
 const avoidCount=useMemo(()=>rows.filter(isAvoid).length,[rows]);
 const visible=useMemo(()=>rows.filter(row=>filter==="avoid"?isAvoid(row):filter==="better"?row.reaction==="better"&&!isAvoid(row):filter==="neutral"?row.reaction==="neutral"&&!isAvoid(row):true),[rows,filter]);
 const filters:Array<{key:HistoryFilter;zh:string;en:string}>=[{key:"all",zh:"全部记录",en:"All"},{key:"avoid",zh:`避雷${avoidCount?` · ${avoidCount}`:""}`,en:`Avoid${avoidCount?` · ${avoidCount}`:""}`},{key:"better",zh:"用着不错",en:"Worked well"},{key:"neutral",zh:"感觉一般",en:"Neutral"}];
 const reactionLabel={better:t("皮肤有改善","Skin improved"),neutral:t("没有明显变化","No clear change"),irritated:t("出现刺激 / 不适","Irritation / discomfort")} as const;
 const textureLabel={love:t("喜欢肤感","Liked texture"),okay:t("肤感一般","Texture was okay"),dislike:t("不喜欢肤感","Disliked texture")} as const;
 const repurchaseLabel={yes:t("会回购","Would repurchase"),maybe:t("可能回购","Maybe repurchase"),no:t("不会回购","Would not repurchase")} as const;
 return <section style={{border:`1px solid ${LINE}`,borderRadius:17,background:"white",padding:17,marginBottom:14}}>
  <div style={{display:"flex",alignItems:"flex-start",justifyContent:"space-between",gap:12,marginBottom:12}}><div><div style={{display:"flex",alignItems:"center",gap:7,color:SAGE,fontSize:11,fontWeight:700,marginBottom:5}}><History size={15}/>{t("我的记录","My history")}</div><div style={{color:MUTE,fontSize:11.5,lineHeight:1.55}}>{t("查看你发布过的 Routine 和自己的产品反馈；踩过的雷会继续影响推荐排序。","Review your routine posts and product feedback. Avoid-list feedback continues to influence recommendations.")}</div></div><button onClick={()=>void load()} disabled={loading} aria-label={t("刷新","Refresh")} style={{border:`1px solid ${LINE}`,borderRadius:"50%",width:32,height:32,background:"white",display:"grid",placeItems:"center",color:MUTE,cursor:loading?"default":"pointer",flex:"0 0 auto"}}><RefreshCw size={14}/></button></div>
  <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:6,marginBottom:14}}><button onClick={()=>setSection("feedback")} style={{border:`1px solid ${section==="feedback"?SAGE:LINE}`,borderRadius:999,padding:"8px 10px",background:section==="feedback"?"#EDF1EA":"white",color:section==="feedback"?"#31563C":MUTE,fontSize:11,fontWeight:650}}>{t("Feedback 记录","Feedback")}</button><button onClick={()=>setSection("posts")} style={{border:`1px solid ${section==="posts"?SAGE:LINE}`,borderRadius:999,padding:"8px 10px",background:section==="posts"?"#EDF1EA":"white",color:section==="posts"?"#31563C":MUTE,fontSize:11,fontWeight:650}}>{t("我的帖子","My posts")}</button></div>
  {loading?<div style={{color:MUTE,fontSize:11.5,padding:"10px 0"}}>{t("正在读取记录…","Loading history…")}</div>:error?<div style={{color:RUST,fontSize:11.5,lineHeight:1.5}}>{error}</div>:section==="posts"?<div style={{display:"grid",gap:9}}>{posts.length===0?<div style={{border:`1px dashed ${LINE}`,borderRadius:12,padding:14,color:MUTE,fontSize:11.5,lineHeight:1.55}}>{t("还没有发布或保存过 Routine。","You have not created any routine posts yet.")}</div>:posts.map(post=><article key={post.id} style={{border:`1px solid ${LINE}`,borderRadius:13,padding:13,background:"#FFF"}}><div style={{display:"flex",justifyContent:"space-between",gap:10}}><div style={{minWidth:0}}><div style={{display:"flex",gap:6,alignItems:"center",fontSize:10,color:SAGE,marginBottom:4}}><FileText size={13}/>{post.visibility==="public"?t("已公开","Public"):t("仅自己可见","Private")}</div><div style={{fontSize:13,color:INK,fontWeight:650,overflowWrap:"anywhere"}}>{post.title}</div></div>{post.overallRating?<div style={{fontSize:10,color:SAGE,whiteSpace:"nowrap"}}>{"★".repeat(post.overallRating)}{"☆".repeat(5-post.overallRating)}</div>:null}</div>{post.description&&<div style={{marginTop:8,color:MUTE,fontSize:11,lineHeight:1.55}}>{post.description}</div>}<div style={{display:"flex",gap:5,flexWrap:"wrap",marginTop:8}}>{post.goals.slice(0,4).map(goal=><span key={goal} style={{border:`1px solid ${LINE}`,borderRadius:999,padding:"4px 7px",fontSize:9.5,color:MUTE}}>{goal}</span>)}</div><div style={{marginTop:8,color:"#9A9389",fontSize:9.5}}>{new Date(post.publishedAt||post.createdAt).toLocaleDateString()}</div></article>)}</div>:<><div style={{display:"flex",gap:6,flexWrap:"wrap",marginBottom:14}}>{filters.map(item=><button key={item.key} onClick={()=>setFilter(item.key)} style={{border:`1px solid ${filter===item.key?(item.key==="avoid"?"#D7A99D":SAGE):LINE}`,borderRadius:999,padding:"7px 10px",background:filter===item.key?(item.key==="avoid"?"#FFF5F2":"#EDF1EA"):"white",color:filter===item.key?(item.key==="avoid"?RUST:"#31563C"):MUTE,fontSize:10.5,cursor:"pointer"}}>{t(item.zh,item.en)}</button>)}</div>{visible.length===0?<div style={{border:`1px dashed ${LINE}`,borderRadius:12,padding:14,color:MUTE,fontSize:11.5,lineHeight:1.55}}>{filter==="avoid"?t("目前没有避雷记录。以后你把产品标记为刺激、不喜欢肤感或不会回购，就会自动出现在这里。","No avoid-list products yet. Products marked as irritating, disliked in texture, or not worth repurchasing will appear here."):t("还没有产品使用记录。去产品详情页提交一次使用反馈后，这里会自动保存。","No product history yet. Submit feedback on a product page and it will appear here automatically.")}</div>:<div style={{display:"grid",gap:9}}>{visible.map(row=>{const avoid=isAvoid(row);return <article key={row.id} style={{border:`1px solid ${avoid?"#E7C8C0":LINE}`,borderRadius:13,padding:13,background:avoid?"#FFF9F7":"#FFF"}}><div style={{display:"flex",justifyContent:"space-between",gap:10,alignItems:"flex-start"}}><div style={{minWidth:0}}><div style={{fontSize:10,color:SAGE,marginBottom:3}}>{row.brand}</div><div style={{fontSize:13,color:INK,fontWeight:650,overflowWrap:"anywhere"}}>{row.product_name}</div></div><div style={{display:"inline-flex",alignItems:"center",gap:5,flex:"0 0 auto",color:avoid?RUST:"#4E6254",fontSize:10,fontWeight:650}}>{avoid?<ShieldAlert size={14}/>:row.reaction==="better"?<CheckCircle2 size={14}/>:<AlertTriangle size={14}/>} {avoid?t("避雷","Avoid"):reactionLabel[row.reaction]}</div></div><div style={{display:"flex",gap:5,flexWrap:"wrap",marginTop:9}}>{[reactionLabel[row.reaction],textureLabel[row.texture],repurchaseLabel[row.repurchase]].map(label=><span key={label} style={{border:`1px solid ${LINE}`,borderRadius:999,padding:"4px 7px",color:MUTE,fontSize:9.5}}>{label}</span>)}</div>{row.note&&<div style={{marginTop:9,color:"#514C45",fontSize:11,lineHeight:1.55}}>{row.note}</div>}<div style={{marginTop:8,color:"#9A9389",fontSize:9.5}}>{new Date(row.updated_at).toLocaleDateString()}</div></article>})}</div>}</>}
 </section>;
}
